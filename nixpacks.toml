[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = "sh -lc '/mise/shims/node node_modules/next/dist/bin/next start -p ${PORT:-3000} -H 0.0.0.0'"
